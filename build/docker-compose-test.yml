version: '3.3'

networks:
    app-net:
        external:
            name: nat
         
services:
    donaldonsoftware:
        image: t3winc/donaldonsoftware:${semver}
        container_name: qa.donaldonsoftware
        restart: always
        expose:
            - 80
        environment: 
            - "VIRTUAL_HOST=qa-blog.t3winc.com"
            - "LETSENCRYPT_HOST=qa-blog.t3winc.com"              
        networks: 
            - app-net